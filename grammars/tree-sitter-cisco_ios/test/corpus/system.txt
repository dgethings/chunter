============
hostname
============

!
! version 26.1.0
!
hostname test
!

---

(config
  (running_version)
  (hostname
    (value)))

===
alias
===

!
! version 26.1.0
!
alias exec siib show ip interface brief
!

---

(config
  (running_version)
  (alias_statement
    (mode)
    (alias)
    (command)))

===
two aliases
===

!
! version 26.1.0
!
alias exec siib show ip interface brief
alias exec sibnb show ip bgp neighbor brief
!

---

(config
  (running_version)
  (alias_statement
    (mode)
    (alias)
    (command))
  (alias_statement
    (mode)
    (alias)
    (command)))
