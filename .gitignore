.ts-gen-stamp
bin/
dist/
log.log
example.ios.j2
.nvim.lua

# Go workspace (machine-local; auto-generated by `make` to point the Go
# toolchain at the sibling grammar module — see Makefile `workspace` target)
go.work
go.work.sum

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
